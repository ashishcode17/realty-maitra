# Database - use one of these (see DATABASE_ALTERNATIVES.md if Neon has connectivity issues)
# Option A: Local Postgres via Docker - postgresql://postgres:postgres@localhost:5432/postgres
# Option B: Supabase / Neon / any Postgres - postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/postgres"

# JWT Secret (change this to a random string in production)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# App URL (production: https://realtymaitra.propertywithmanish.com)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Set to "true" in production to hide demo credentials on login/register
NEXT_PUBLIC_HIDE_DEMO="false"

# Email (OTP and welcome emails)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="noreply@realtymaitra.com"

# --- Firebase Phone Auth (used for register + login OTP) ---
# 1. Create project at https://console.firebase.google.com
# 2. Enable Authentication → Sign-in method → Phone
# 3. Project Settings → General → Your apps → Add web app → copy config
NEXT_PUBLIC_FIREBASE_API_KEY=""
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=""
NEXT_PUBLIC_FIREBASE_PROJECT_ID=""
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=""
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=""
NEXT_PUBLIC_FIREBASE_APP_ID=""
# 4. Project Settings → Service accounts → Generate new private key → paste JSON as one line in Vercel
FIREBASE_SERVICE_ACCOUNT_JSON=""
# For live/serverless: Govt ID uploads use Firebase Storage. Set bucket name (e.g. your-project.appspot.com or your-project.firebasestorage.app).
FIREBASE_STORAGE_BUCKET=""

# SMS – India: set one or more. App tries Fast2SMS → 2Factor → Twilio
# FAST2SMS_API_KEY=""
# TWO_FACTOR_API_KEY=""   # 2factor.in – often works when Fast2SMS fails

# SMS – International (and India fallback): Twilio
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""
